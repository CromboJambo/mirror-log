name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      BIN_NAME: mirror-log
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: clippy,rustfmt

      - name: Cache cargo registry
        uses: actions/cache@v3
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-registry-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache cargo index
        uses: actions/cache@v3
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-git-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache cargo build
        uses: actions/cache@v3
        with:
          path: target
          key: ${{ runner.os }}-build-${{ hashFiles('**/Cargo.lock') }}

      - name: Build release
        run: cargo build --release --all-features --locked

      - name: Run tests
        run: cargo test --all-features --locked

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --all-features --locked -- -D warnings

      - name: Verify tag matches Cargo.toml version
        shell: bash
        run: |
          set -euo pipefail
          CARGO_VERSION="$(sed -n 's/^version = \"\(.*\)\"$/\1/p' Cargo.toml | head -n1)"
          TAG_VERSION="${GITHUB_REF_NAME#v}"

          if [[ -z "$CARGO_VERSION" ]]; then
            echo "Failed to read package version from Cargo.toml" >&2
            exit 1
          fi

          if [[ "$CARGO_VERSION" != "$TAG_VERSION" ]]; then
            echo "Tag ${GITHUB_REF_NAME} does not match Cargo.toml version ${CARGO_VERSION}" >&2
            exit 1
          fi

      - name: Create release archive
        shell: bash
        run: |
          set -euo pipefail
          ARCHIVE_ROOT="dist/${BIN_NAME}-${GITHUB_REF_NAME}-linux-x86_64"
          mkdir -p "$ARCHIVE_ROOT"
          cp "target/release/${BIN_NAME}" "$ARCHIVE_ROOT/"
          cp LICENSE README.md "$ARCHIVE_ROOT/"
          tar -C dist -czf "target/${BIN_NAME}-${GITHUB_REF_NAME}-linux-x86_64.tar.gz" \
            "${BIN_NAME}-${GITHUB_REF_NAME}-linux-x86_64"

      - name: Publish to GitHub
        uses: softprops/action-gh-release@v1
        with:
          files: target/${{ env.BIN_NAME }}-${{ github.ref_name }}-linux-x86_64.tar.gz
          generate_release_notes: true

      - name: Publish to crates.io
        if: ${{ secrets.CARGO_TOKEN != '' }}
        run: cargo publish --token ${{ secrets.CARGO_TOKEN }}
